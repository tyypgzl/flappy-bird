class GameConst {
  GameConst._();
  /// Bird width-height
  static const double birdSize = 50;

  /// Height to jump up when clicked.
  static const double jumpHeight = 60;

  /// Falling speed
  static const double gravity = 140;

  /// Parallax sliding speed
  static const double gameSpeed = 100;

  static const double obstacleWidth = 80;

  static const double obstacleHeight = 550;
}
